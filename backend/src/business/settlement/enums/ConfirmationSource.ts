/**
 * Enum representing the origins of settlement confirmation signals.
 */
export enum ConfirmationSource {
  BLOCKCHAIN = "BLOCKCHAIN",
  BANK = "BANK",
  GATEWAY = "GATEWAY",
  TREASURY = "TREASURY"
}
