/**
 * Enum representing the operational state of an Organization.
 */
export enum OrganizationStatus {
  PENDING = "PENDING",
  ACTIVE = "ACTIVE",
  SUSPENDED = "SUSPENDED",
  ARCHIVED = "ARCHIVED",
  DELETED = "DELETED"
}
