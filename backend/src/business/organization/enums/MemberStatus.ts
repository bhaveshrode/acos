/**
 * Enum representing the status of a User's membership inside an Organization.
 */
export enum MemberStatus {
  INVITED = "INVITED",
  ACTIVE = "ACTIVE",
  SUSPENDED = "SUSPENDED",
  REMOVED = "REMOVED"
}
