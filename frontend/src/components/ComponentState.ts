/**
 * ComponentState enum capturing components rendering lifecycle states.
 */
export enum ComponentState {
  Created = "Created",
  Mounted = "Mounted",
  Updating = "Updating",
  Unmounted = "Unmounted"
}
